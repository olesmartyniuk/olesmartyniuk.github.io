---
title: Пост 1
date: 2020-05-05 08:57:00 Z
categories:
- ".net"
tags:
- tag1
- tag2
---

## Code

Inline `code`

Indented code

    // Some comments
    line 1 of code
    line 2 of code
    line 3 of code


Block code "fences"

```c#
namespace Scania.Sws.EcuUpdate
{
    public class Startup
    {
        public IConfiguration Configuration { get; }

        public Startup(IConfiguration configuration)
        {
            Configuration = configuration;
            Logging.Setup(Configuration);
        }

        // This method gets called by the runtime. Use this method to add services to the container.
        public void ConfigureServices(IServiceCollection services)
        {
            // This call adds and configures the set of standard services, as enforced by Yeoman generator
            AppConfigure.ConfigureServices(Configuration, services);

            // Add project specific services below
            services.AddSwaggerGenNewtonsoftSupport();

            services.AddSingleton<IUserService, UserService>();
            services.AddSingleton<IUpdateService, UpdateService>();
        }

```
